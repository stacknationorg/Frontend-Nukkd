import React from 'react'
import Title from "../../shared/Title"
import {Routes} from "../../shared/Routes"
import { Name } from '../../shared/config'

function Stores() {
    return (
        <>
            <Title title={`${Name} | ${Routes.stores.name}`}/>
            <div>
                Work in progress
            </div>
        </>
    )
}

export default Stores
