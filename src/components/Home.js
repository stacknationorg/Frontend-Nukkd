import React from 'react'
import Title from "../shared/Title";
import { Name } from '../shared/config';

function Home() {
    return (
        <>
            <Title title={`${Name}`}/>
            <div>
                Work in progress
            </div>
        </>
    )
}

export default Home
